{
  "name": "rescript-esbuild-template",
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently --names rescript,esbuild,server -c red.bold,blue.bold,green.bold --kill-others \"NINJA_ANSI_FORCED=1 bsb -make-world -w\" \"esbuild src/App.bs.js --outfile=www/dist/bundle.js --bundle --sourcemap --target=chrome58,firefox57,safari11,edge16 --define:process.env.NODE_ENV='\\\"development\\\"' --watch\" \"serve www\"",
    "build:production": "bsb -make-world && esbuild src/App.bs.js --outfile=www/dist/bundle.js --bundle --minify --sourcemap --target=chrome58,firefox57,safari11,edge16 --define:process.env.NODE_ENV='\"production\"'",
    "clean": "bsb -clean-world && rimraf www/dist"
  },
  "keywords": [
    "rescript",
    "esbuild"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "*",
    "concurrently": "^5.3.0",
    "esbuild": "^0.8.45",
    "rimraf": "^3.0.2",
    "serve": "^11.3.2"
  },
  "dependencies": {
    "@rescript/react": "^0.10.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
